services:
  - type: web
    name: yt-transpose
    runtime: node
    plan: free
    branch: claude/youtube-song-transposer-bq961f
    buildCommand: npm install && bash scripts/install-yt-dlp.sh
    startCommand: npm start
